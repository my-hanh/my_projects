package com.zhaw.it.pm3.tournamentgenerator.persons;

/**
 * Represents an overseer in the tournament system.
 *
 * Currently, this class serves as a placeholder and inherits from {@link Person}.
 * The overseer is expected to have specific responsibilities and functionalities
 * in supervising the tournament, but these are not yet implemented.
 */
public class Overseer extends Person {

    /**
     * Constructs an {@code Overseer} object with the specified name.
     *
     * @param name the name of the overseer.
     */
    public Overseer(String name) {
        super(name);
    }
}